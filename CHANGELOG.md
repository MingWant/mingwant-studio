# CHANGELOG

## Unreleased

- 修复 Grok Imagine 图片编辑协议：新增独立的 xAI Images JSON 请求方式，单图使用 `image`、多图使用 `images`，不再把参考图错误发送为 multipart；既有误标为 OpenAI Images 的 Grok 图片模型也会直接按模型名走正确报文，生成参数映射为 xAI 的画幅与 1k/2k 分辨率，临时结果会立即进入现有资源落盘流程。
- 修复 Grok Imagine 视频协议兼容：既有误标为 OpenAI Compatible Videos 的官方模型会直接改走 JSON `POST /v1/videos/generations`，不再请求 legacy `/videos`；图生视频选择自动画幅时保留起始图比例，`expired` 状态会作为失败终态停止轮询，并按官方范围提供 480p/720p/1080p。
- 修复分镜节点与画布基础交互：无可滚内容的分镜表不再吞掉画布缩放，损坏的 Unicode 替换字符不会继续显示或进入后续提示词；节点避障连线改为圆滑转角，中键/Space 平移结束后不再回弹或残留抓取光标。图片生成不再误读视频能力配置，可空引用数据统一收敛，并在任务提交前用中文提示模型参数异常；分镜新增可见的全局/单镜头角色与参考图入口、协议预检、角色版本锁定和生成结果引用标识。
- 系统优化自由画布与工作台 UI/UX：节点与背板改为稳定平面表面，空白左拖框选、Space/中键平移；自动落点继承背板并按需扩容，流程布局支持环路和外部障碍，连线增加方向、实时合法性反馈、节点避障与视口裁剪，生成结果不再强制改变缩放。节点、背板、小地图与尺寸调整补齐触屏、键盘和读屏入口；首页与提示词交接改为真实动作，积分/任务/技能页区分加载失败、刷新失败与真实空数据，积分卡移除 3D 倾斜效果。
- 解除在线 Agent 的网页固定输出上限：Chat Completions、Responses 和 Gemini 工具编排不再注入 2048/4096 token 限制，仍会拒绝供应商明确标记的截断或未完整工具参数。
- 新增 Dola 网页工作台到 New API Channel 1 的独立视频适配器 MVP：通过 Playwright 持久浏览器会话提交 Seedance 2.5 文本生视频，提供 `/v1/videos` 创建/轮询/结果下载和账号管理接口；动态风控参数不落盘，只有明确提交前额度不足才换号，额度耗尽账号按配置的每日刷新点自动恢复。
- Dola Adapter 新增同源账号池管理 UI：可用管理 Key 登录，查看 Worker/账号/额度状态，添加独立 Profile、打开可见 Dola 登录浏览器并启用或停用账号；管理 Key 仅保留在当前浏览器标签页会话中。
- 系统优化：对齐上游模型能力配置思路，为视频模型增加服务端事实源；管理员可维护提示词、参考媒体、时长、比例、分辨率、音频/水印和生成模式约束，任务会在冻结积分前统一校验，画布视频设置与生成模式按当前模型动态展示合法选项，并统一归一化比例、分辨率和时长步长。
- 对齐上游分镜 Composer 的模型绑定改进：脚本节点现在可直接选择文本模型并把选择保存到节点，分镜生成、重试和手动交付沿用同一渠道与模型；同时收紧窄节点输入的横向溢出。
- 修复创作台对已填写 `/v1beta` 版本根地址的识别：不会再把 OpenAI 兼容请求错误拼成 `/v1beta/v1/...`；同时在自定义渠道和系统渠道配置页明确 Base URL 应填写根地址/版本根地址，而不是 Postman 中的完整接口路径。
- 修复 Backend 对大小写变体版本根地址的识别，避免 `/V1` 或 `/V1BETA` 被再次追加 `/v1`。
- 改善创作台上游错误提示：404 会引导检查 Base URL 与版本路径，400/422 会明确提示协议或 Function Calling 请求体差异，避免把 Postman 的普通文本成功误判为画布 Agent 工具调用兼容。
- 修复管理员测活误触发限流：文本测活与自动工具诊断改用独立的低频窗口，并按渠道、模型和协议配置分桶；管理员同一配置每类诊断 10 分钟内可检查 10 次，无效 JSON 不再消耗测活额度，旧的共用限流窗口也不会继续影响升级后的测活。
- 澄清测活输出预算：256/512 token 只属于低成本文本与工具探针，不再在界面上暗示为模型或创作台的上下文上限；正式在线 Agent、后台分镜和模型上下文继续使用各自的请求策略。
- 调整测活权限语义：LLM/工具测活现在只作为管理员查看渠道可用性的诊断记录；系统渠道和自定义渠道即使未测活、测活失败或已过期，普通用户仍可在费用确认后调用、查看真实失败状态并按安全规则重试，不再被探针结果直接拦截。
- 构建修复：补充 SSE 正文识别的显式事件流状态，避免 TypeScript 将闭包内的 `mode` 赋值误判为不可达比较。
- 构建修复：修正前端探针状态可选配置、SSE 模式收窄、Chat 兼容流 `delta` 和 Gemini 事件类型的 TypeScript 定义；Docker Web 镜像不再因这 6 个类型错误停在 `bun run build`。
- 可用性：系统渠道的 LLM 测活仍允许管理员先验证上游而不扣积分，但创作台、任务中心和视频任务现在会在出站前检查该模型是否已配置用户积分价格；旧画布或旧标签页再遇到“测活成功、正式调用失败”时会明确提示管理员补定价，本次不会创建任务或调用供应商。
- 稳定性：普通画布文本任务现在也会在后端请求体中使用有界输出；Chat Completions 的 Kimi K3/K3-LS 同时带低推理档位，避免测活成功后创作台文本节点仍以无界推理长时间等待并撞上游 524。
- 修复文本测活与工具诊断共用活动互斥键时的状态串写：工具诊断排队期间，另一标签页的“重新测活”不再把工具结果记录成普通文本绿灯，而是明确返回未调用供应商的冲突提示。
- 稳定性：账号作用域的本地/会话缓存在隐私模式或浏览器扩展拦截时安全降级；配置、测活和 Agent 不再因缓存读写异常在调用前抛错，注册成功也不会因一次性引导标记写入失败而被误报为注册失败。
- 安全：活动账号作用域改为按浏览器标签页隔离，避免不同账号标签页互相覆盖本地配置、密钥会话区和测活状态。
- 稳定性：本地 Canvas Agent 面板尺寸偏好读写失败时改用默认宽度，不再让隐私模式或浏览器扩展拦截 `localStorage` 导致画布初始化失败。
- 稳定性：工作台侧栏、管理员侧栏、模型配置引导和节点快捷工具的非业务偏好改为安全降级；浏览器禁用本地/会话存储时仍可进入页面、操作节点和调用 Agent。
- 稳定性：项目章节与画布项目侧栏的选中章节、滚动位置改为安全会话偏好；会话存储不可用时仍可打开项目、创建画布和编辑章节。
- 将新用户画布默认模式收敛为“手动交付”：首次进入短剧入口先只生成可编辑分镜、首帧图片提示词和视频动作提示词，不直接进入慢速长分镜或视频任务；已保存的专业/简洁模式不被覆盖，可从画布顶部切换。
- 统一任务中心与画布的慢兼容模型策略：Kimi K3-LS、K2.7 Code/K2.6 等已知兼容别名从任务中心创建 Agent 分镜时也会改为手动交付提示，不再创建容易触发上游 524 的后台长 JSON 任务。
- 调整完整影视分镜的测活语义：文本 SSE 与无副作用 Function Calling 诊断继续用于管理员判断渠道能力和费用确认提示；诊断未完成、过期或失败时普通用户仍可明确确认并发起真实请求，真实工具响应、超时和费用状态决定后续处理，不再因探针状态直接拦截创作。
- 收敛慢推理模型的正式分镜输出契约：默认优先完整生成 3 个短镜头，限制每镜提示词和边界字段长度，避免模型在重复风格说明上耗尽输出预算；首帧图片提示词、视频动作提示词和可编辑结构仍保留。
- 针对 Kimi K3-LS、K2.7 Code/K2.6 兼容别名的短剧入口增加可用降级：识别到“一句话生成影视项目”时直接走一次有界纯文本分镜整理，写入可复制的 script 节点，不再把慢兼容模型送入容易 524 的后台长 JSON 任务；官方 K3、Responses 和 Gemini 路径保持原有长分镜流程。
- 收紧短分镜交付提示：默认只生成 3 个镜头并限制每条图片/视频提示词长度，优先在一次模型调用内闭合结果，减少 Kimi 兼容网关长时间隐藏推理后又 524 的情况。
- 扩大短剧入口的可用性：已知慢兼容别名仍可在费用确认后改走一次有界纯文本分镜整理；其它模型即使未测活、测活失败或过期，也允许普通用户发起完整影视任务，测活只作为管理员风险提示。
- 加固短分镜纯文本路径：即使兼容网关无视无工具请求返回函数调用，也会直接停止且不执行画布/视频操作，避免异常协议绕过手动交付边界。
- 修复浏览器 Gemini Agent 把 `thought: true` 思考片段混入可见正文和分镜解析的问题；现在与 Backend 使用同一过滤口径，同时保留同一响应中的合法函数调用。
- 补齐浏览器 Gemini 兼容协议的 `finish_reason` 与 `function_call` 解析；不降低终态和完整性校验，避免部分网关把已完成结果误判为缺少完成标记。
- 加固兼容网关同时返回 camelCase/snake_case 工具字段的解析：空的 `tool_calls`、`functionCall` 不再遮蔽另一种格式的合法调用，重复同 ID 的终态调用只保留一次，避免测活通过后创作台误报“没有工具”或重复执行。
- 同步修正后台 Chat 工具诊断对 `functionCall`/`function_call` 的识别；兼容网关只返回 camelCase 函数调用时，工具测活不再误报失败，测活结论与创作台实际调用协议保持一致。
- 加固系统渠道测活的配置绑定：任务提交时保存不可逆配置指纹，排队或执行期间密钥、协议、模型变化会阻止旧任务调用或写入新配置绿灯；测活记录使用真实供应商请求开始时间，迟到结果只能绑定原配置并要求重新测活。
- 调整失败任务重试的费用门禁：408/499/524、连接中断和 5xx 的文本/图片/音频/无上游任务 ID 分镜，在用户明确确认可能重复计费后可自助创建新的尝试；旧订单继续保留给管理员核账，不再把自用工作台锁进审核死循环。已有成功调用记录或可查询上游任务的视频仍必须先恢复或手动查询。
- 修复创作台在“非流式测活通过”时仍强制发送 `stream=true` 的问题；手动短文本和已明确只允许一轮的短 Agent 会按最近测活结果改用普通 JSON 请求，兼容只支持非流式文本/工具接口的上游，长影视与长分镜仍从 `stream=true` 开始并按真实响应完整性处理。
- 扩展手动短剧文本解析：兼容“首帧/分镜图提示词”、视频生成提示词、英文下划线字段和常见表格表头，模型返回字段名略有差异时仍能写入可复制的分镜脚本。
- 加固手动交付边界：在线、工具桥接和本地 Agent 都会在写入视频节点、视频配置或视频生成操作前停止，不会因兼容模型误返回工具参数而绕过“复制提示词后到网页工作台提交”的流程。
- 修复手动交付短剧入口依赖 Function Calling 的阻塞：现在只发起一次有界短文本请求，浏览器可把 JSON、Markdown 或“镜头/画面/动作”格式解析为可编辑 script 节点；文本测活成功但工具协议不兼容时仍能直接得到分镜提示词，不创建后台长分镜或视频任务。
- 修正手动交付模式的短剧入口：无论文本测活是否显示渐进式 SSE，手动模式都只发起一次有界 Agent 调用写入 3–5 镜头的可编辑 script 节点，不再因为测活通过而进入后台长分镜任务；这样流程稳定停在首帧图片提示词和视频动作提示词，直接复制到网页工作台逐镜制作。
- 给“手动交付”增加非流式模型的可用降级：长分镜请求仍从 SSE 开始并对非流式结果显示风险，当前模型只有非流式测活时，短剧入口会用一次有界 Agent 调用直接创建 3-5 镜头的可编辑 script 节点，不创建后台长任务或视频任务，用户可复制分镜图/视频提示词到网页工作台继续制作。
- 加固在线与本地 Agent 创建/更新分镜脚本节点的共同写回：模型省略镜头 ID、时长或提示词字段时，画布会补齐唯一行身份和可编辑默认字段，避免脚本节点写入后无法编辑、复制或连接镜头。
- 明确手动交付的复制入口：分镜脚本的手动交付包本来就同时包含首帧图片提示词和视频提示词，现在按钮与成功提示会直说“视频/首帧提示词”，用户可整包复制到网页工作台，不新增模型调用。
- 针对 Kimi/Kimi 兼容 Chat 接口增加按意图收窄的 Agent 工具档：只读请求首轮仅发送只读工具，短剧/分镜请求只发送影视入口，普通写入使用 `kimi-write`，生成请求使用 `kimi-generation`，未识别意图才回退 `kimi-compact`，避免单函数测活通过后因二十多个深层 Schema 体积过大而在真实调用阶段 400 或长时间不出首个工具；其它模型与 Responses/Gemini 协议继续使用完整工具集。
- 进一步缩短 Kimi 短剧首轮请求：影视工具调用不再附带完整画布快照和无关读取 Schema，后台影视任务仍接收完整快照并负责分镜生成，降低慢推理网关在工具首包前触发 524 的概率。
- 针对 Kimi K3-LS 兼容别名收窄在线 Agent 的网页输出上限到 2048 token；官方 K3 仍保留 4096 token，降低第三方兼容网关长时间无首包或 524 的概率，长分镜继续使用后台持久任务上限。
- 修复在线 Agent 工具确认后的恢复路径没有持久化首轮 Kimi 工具档的问题；刷新页面或接管待确认消息时会复用 `kimi-read` / `kimi-write` / `kimi-generation` / `kimi-cinematic` 等原档位，不再退回完整工具目录。
- Linux 源码和镜像安装器现在会在拉取源码、部署配置或 GHCR 镜像前预检 Docker Engine、Linux 容器模式以及 Compose 的 `--wait` 能力；Engine 未启动或 Compose 版本过旧时会立即停止，不再留下半套安装现场。
- 修复本地 Docker 构建阻断：Backend 日志包名遮蔽、在线 Agent 工具回复类型推断、测活提交键严格类型和管理员视频任务查询字段现在与实际代码一致，构建失败时不会停在这些编译错误。
- 将 Agent 创作台的当前文本模型测活入口放到模型选择器旁边，用户无需离开画布即可重新测活并查看当前渠道的流式结果；仍沿用原有提交幂等、工具诊断、响应完整性和费用安全边界。
- 在创作台模型选择器旁显示独立的“工具通过 / 工具未通过 / 需测工具”状态，并明确提示文本测活后还要运行无副作用工具诊断，减少“LLM 测活成功但 Agent 调用失败”的误判。
- 文本测活成功后自动追加一次固定的无副作用工具诊断，测活结束时直接给出“创作台工具可用/未通过”结论，减少用户看到文本绿灯后误把 Function Calling 当成已验证的问题；工具诊断仍有独立幂等键、512 token 上限且不执行画布操作。
- 修复普通用户在创作台对系统渠道点击测活必然收到权限错误的问题；系统渠道测活按钮现在按管理员权限禁用并给出明确说明，普通用户继续使用管理员发布的模型能力状态或切换到自定义渠道测活。
- 将系统渠道的无副作用工具诊断结论写入模型目录并按配置指纹共享给普通用户；管理员测活后，普通用户不再因为本地没有管理员浏览器记录而看到“需测工具”并直接踩到 Function Calling 失败。
- 系统渠道在线 Agent 在工具诊断明确失败时显示风险并默认只发 1 次；尚未完成诊断的短 Agent 同样允许带风险尝试，完整影视分镜也可在费用确认后调用，真实工具响应决定是否执行画布写操作。
- 修复创作台内嵌测活完成后能力标签不即时刷新的问题；文本或工具诊断写入当前标签页后会立即重新计算状态，切换模型/协议时也会清除上一配置的探针结果，避免把旧绿灯误认为新模型可用。
- 修复模型级协议覆盖渠道默认格式时测活结论无法被创作台命中的问题；本地测活索引现在按实际请求协议推导 OpenAI/Gemini 格式，与探针和创作台发送路径保持一致。
- 加固测活提交键绑定：API Key、Base URL、渠道格式或默认协议变化时轮换不含密钥的配置版本；响应丢失后重新测活不会把旧提交键带到新连接并被后端拒绝。
- 修复损坏的 AI 配置快照阻断账号恢复：普通配置 JSON 无法解析时安全清理快照并回到默认配置，独立会话/持久密钥区不会被带入普通快照或让画布初始化崩溃。
- 修复创建任务时先构造计费订单再保护/序列化输入的问题；密钥保护或输入持久化失败时不再进入计费流程，并返回稳定错误提示。
- 收紧测活任务的保存失败边界：加密或 JSON 持久化异常只返回稳定的“未调用供应商”提示，不再把设置密钥路径等底层错误暴露到接口。
- 收紧 SQLite 存储迁移和角色三视图收尾的 JSON 写入：任何媒体、密钥或角色元数据序列化失败都会中止本次写入并保留明确错误，不再静默保存空正文。
- 修复系统渠道慢测活在管理员改端点/协议/凭据后仍被新请求复用旧活动任务的问题；现在按提交时配置指纹核对，旧任务执行期间只返回明确冲突，不会把旧结果绑定到新配置。
- 修复系统渠道配置被管理员更新后浏览器仍沿用旧本地绿灯的问题；当 Backend 没有当前模型的共享测活结论时，前端现在显示未验证风险，不会把旧状态误当成当前连接证明，但普通用户仍可在费用确认后调用。
- 收紧本地 Canvas Agent 启动凭据边界：旧版 `?agentToken=` 链接现在只会被清理并提示更新插件，不再读取或写入会话 Token；只有 `#agentUrl=...&agentToken=...` fragment 才能交接凭据。
- 收紧旧版工具诊断结论：缺少当前校验版本号的“工具通过”记录现在显示为“工具需重测”，不会把历史 Schema/参数规则当作当前创作台能力。
- Agent 正在等待或执行当前请求时会锁定创作台内嵌测活按钮，避免用户在同一轮预算之外并发启动另一笔供应商探针；设置页的独立测活不受影响。
- 两种 Linux 服务器安装器遇到未固定或非法的 `COMPOSE_PROJECT_NAME` 时会在 Compose 启动前停止，要求先人工核对原项目名和数据卷，避免旧 `.env` 按目录名切换到另一套卷。
- 修复局域网 HTTP 页面缺少 `crypto.randomUUID()` 时本地 Canvas Agent 连接和画布事件卡死：客户端 ID 现在统一使用可降级生成器，不会在真正发送 Agent 或画布操作前因浏览器能力缺失而失败。
- 修复局域网 HTTP 页面缺少 `crypto.subtle` 时影视入口在供应商调用前被配置指纹检查拦截：现在使用不含 API Key 的本地降级指纹，服务端仍按真实配置、价格和费用安全边界校验，测活只作为诊断记录。
- 修复任务中心把不确定请求显示成可重试状态：自定义渠道或旧任务即使缺少 `billing` 摘要，或计费状态残留为 `failed`，只要错误正文已明确“费用待核对/可能仍在执行”，列表现在优先标记“待核对”，避免用户误以为可以立即重试；原有重试确认和供应商任务查询门禁不变。
- 补齐在线 Agent 的费用风险提示：流式响应损坏、请求状态不确定或上游成功后本地交付失败的稳定错误，现在也会在 Agent 消息中明确提醒可能已计费并先核对账单；调用前明确未出站的请求则显示本次没有供应商费用，避免用户只看到“请求失败”而立即重试。
- 加固在线 Agent 首次发送的进度持久化：用户消息、模型轮次预算和冻结的渠道绑定现在会在调用供应商前先写入本地画布存储；存储失败时明确显示“未调用模型”并保留可处理的错误，不会出现模型已执行但页面没有调用进度的状态。
- 修复前端把系统渠道在创建计费订单前发生的槽位/总时限超时误判为“费用待核对”：识别“尚未创建计费订单或发出上游请求”这类无调用证明后，任务与画布会保留可退款/可重试语义，不再把未发出的请求引导到供应商核账流程。
- 当当前渠道配置的无副作用工具诊断已明确失败时，在线 Agent 现在显示风险并默认只允许 1 次真实调用；未诊断或已过期的配置同样保留明确提示，实际没有返回工具调用时才停止画布写操作，影视后台分镜也可在费用确认后尝试。
- 无副作用工具诊断现在携带代表性画布工具 Schema（数组、枚举、嵌套对象和 metadata），提前识别“扁平工具测活通过、创作台完整工具请求被网关拒绝”的渠道，仍只调用固定探针函数且不执行画布操作。
- 修复 DeepSeek V4 思考模式的创作台工具调用：后台固定诊断与网页 Agent 不再发送会触发 HTTP 400 的 `tool_choice` 字段，首轮仍由本地校验要求真实工具调用；后续轮次会在内存中原样回传该协议要求的 `reasoning_content`。
- 修复工具诊断任务实际完成后弹窗没有反馈的问题：终态现在立即提示通过或失败，管理员模型列表刷新后弹窗仍会回显后台保存的最近一次结论、检查时间和过期状态，不再让成功任务看起来像按钮无响应。
- 修复兼容 Chat 模型多轮工具回传误带 `reasoning_content`：只有明确要求续传推理状态的 Kimi/Moonshot 与 DeepSeek V4 保留该字段，其他模型第二轮会剥离未知字段，同时保留工具调用 ID、参数和可见 assistant 内容，避免首轮工具成功后第二轮被网关拒绝。
- 补齐文本测活与创作台工具能力之间的可见性：无副作用 `probe_extract_record` 工具诊断的成功/失败、任务 ID 和时间现在按渠道配置指纹保存，在线 Agent 确认框会直接显示“工具调用已通过/未通过/未验证”，不再把文本链路绿灯误当成 Function Calling 可用；本地记录不包含 API Key、提示词或供应商正文。
- 加固在线 Agent 工具确认后的恢复边界：待确认消息现在保存不含密钥的配置指纹和渠道 ID；页面刷新后只有原模型、渠道、协议、端点和凭据仍一致才允许继续下一轮，否则明确停止并要求重新发送，避免旧工具结果漂移到新渠道。
- 修复创作台浏览器流式解析只信 `Content-Type` 的兼容问题：部分上游虽然返回 `data:`/`event:` SSE，却错误标记为 `application/json`，现在会窥探极短正文前缀后继续按分片解析，避免“LLM 测活成功、创作台请求失败”；真实 JSON 仍按非流式结果处理，未完成流仍进入费用待核对边界。
- 修复兼容网关重复发送累计工具参数导致创作台把合法 JSON 拼坏：Chat Completions 与 Responses 的 SSE 参数合并现在同时支持增量片段和累计片段，首轮工具调用不会因本地参数解析失败而停止。
- 同步修正后台无副作用工具测活的 Chat/Responses 参数合并规则，避免“工具测活通过”与真实创作台解析逻辑不一致。
- 修复测活排队期间编辑渠道会把旧请求结果写到新配置的问题：文本与工具测活现在绑定提交时的端点、协议和凭据版本快照。
- 修复自定义渠道 HTTP 地址测活通过但创作台中转必然拒绝的假绿灯：测活创建与后台执行现在复用创作台 HTTPS 出口校验，设置页也会提前提示必须使用 HTTPS。
- 加固手动交付模式的 Agent 边界：在线与本地 Agent 的视频生成、自动运行流程和 `run_generation` 视频操作现在都会在任务提交前停止，只保留分镜图与视频提示词交付到网页工作台。
- 收紧手动交付模式的底层回调：即使旧画布事件或模式切换后仍触发创建视频节点，也会在写入画布前停止，确保流程真正停在分镜图与视频提示词。
- 补上全屏分镜编辑器的手动交付保护：即使模式切换时残留旧的“生成视频”回调，也会在提交批次前拦截并引导复制视频提示词。
- 修复手动交付模式拦截视频工具后仍继续发起下一轮模型请求的问题：现在会把已生成的分镜结果保留在画布，立即结束本轮 Agent 预算，避免为无法执行的视频工具额外付费。
- 修复画布刷新后的任务恢复把“暂时查不到任务”误标为可重试失败：带任务 ID、任务列表查询失败或创建响应可能丢失时会保留恢复安全锁并自动重查，避免用户在供应商请求状态不明时重复提交。
- 加固分镜批次提交恢复：批次条目在“提交中”时刷新或停止等待，不再因为本页控制器消失而回到 waiting；节点和批次会保留待核对锁，恢复器按节点身份继续找回原任务，避免后台已创建任务后又提交第二笔。
- 补充在线 Agent 慢请求反馈：等待超过 10 秒后显示已等待时长和“请勿重复提交”提示，长推理模型在 524/超时边界前仍能让用户看到请求正在持续跟踪。
- 修复任务页 Agent 分镜确认后的渠道漂移：费用确认弹窗等待期间不再按裸模型名重新解析同名渠道，实际创建任务复用确认时冻结的渠道、协议和凭据。
- 修复短剧分镜批次的模型漂移：分镜图和镜头视频在确认提交时把无密钥的渠道模型标识写入节点，刷新或默认配置变化后仍沿用原选择，渠道失效时明确提示而不会切到另一条同名渠道。
- 修复动作拆分板批次遗漏模型绑定：动作板在确认提交后也会保存无密钥的渠道模型标识，排队期间切换默认模型不会把任务发到另一条同名渠道。
- 修复批量生成提交前配置变化导致的空转：模型被删除、能力撤销或本地准备失败时，批次条目现在明确失败并停止重试，不再在“提交中”和“等待”之间循环，也不会把未确认的请求当成可安全重发。
- 修复多渠道同名模型的任务配置漂移：任务页和画布生成现在复用发送前已经冻结的渠道绑定，并把系统渠道 ID 一并交给 Backend，不会在测活通过后按裸模型名落到另一条 Base URL、协议或 API Key。
- 修复测活成功后创作台仍被误判为未验证：自定义渠道优先使用当前标签页保存的精确测活任务 ID 和完整性版本，系统渠道则按服务端较新的真实观察状态覆盖本地旧结论，避免两类渠道互相遮蔽。
- 阻止旧画布或旧配置中的裸模型名、已删除渠道前缀回退到第一条渠道；同名渠道失配现在明确要求重新选择，避免测活通过后静默改用另一条 Base URL 或 API Key。
- 系统渠道刷新或模型删除后，失效的 `channel::model` 默认值现在会清空并要求重新选择，不会在配置归一化时被悄悄替换成第一个可用模型。
- 模型选择器对失效的渠道绑定明确显示“需重新选择”，避免用户把旧模型名误认为仍指向原渠道。
- 修复浏览器禁止本地存储时测活提交键在请求前抛异常：用户作用域读取/写入现在安全回退，测活会按“未验证”继续而不会把按钮卡在处理中或让创作台抛出未处理错误。
- 修复前后端不同端口的开发代理路径：`VITE_CANVAS_BACKEND_URL` 使用绝对 Backend 地址时，系统渠道的 Chat、Responses 和 Gemini 请求不再错误追加 `/v1` 或 `/v1beta`；Gemini 所需的 `x-goog-api-key` 也已加入精确 CORS 允许头。
- 加固系统渠道幂等键生成：缺少 `crypto.randomUUID` 的旧版或非安全本地浏览器会回退到随机键，不会在供应商请求前因浏览器 API 缺失而失败。
- 修正 Codex `open-canvas` Skill 的本地启动指引：项目使用 Vite 时不再误调用 Next.js，端口冲突时改用真实可用的 `bunx vite --host ... --port ...`。
- 补齐 `open-canvas` Skill 的 Windows 端口归属检查指引，使用 PowerShell 查询监听 PID，避免把其他程序占用的端口误当成画布服务。
- 隔离浏览器测活提交键的账号作用域：持久键改用当前用户存储空间，内存幂等身份也绑定用户，切换账号不会复用旧账号的探针提交。
- 修复 OpenAI 自动兼容文本模型的测活与创作台协议选择不一致：未声明模型级协议时统一使用 Chat Completions，OpenAI Responses 必须显式选择；旧的自动 Responses 测活结论不会继续放行新路径，在线 Agent 日志同时记录实际协议，便于定位工具调用失败。
- 修复后台 Chat/Responses 文本流把 `finish_reason=length/max_tokens` 或 `status=incomplete` 当作成功：普通 JSON、SSE 和非流式回退现在都会拒绝半截正文并进入费用核对边界，分镜不会拿截断 JSON 再发起结构修复。
- 修正混合协议渠道中模型能力覆盖默认协议的情况：文本模型不再继承渠道的视频协议，测活与创作台会使用同一条 Chat 请求路径。
- 修复系统渠道历史混合协议配置：服务端文本代理、后台分镜和测活统一按模型文本能力回退到 Chat/Gemini GenerateContent，避免前端与后端协议判断不一致。
- 加固测活门禁缓存：浏览器隐私模式或扩展拦截本地存储时按“未验证”安全降级，不让创作台点击直接抛出存储异常。
- 在线 Agent 诊断日志补充实际协议、鉴权格式、请求端点、渠道/模型绑定及工具消息数量，定位测活成功但创作台失败时不再只能猜测路径，且不记录密钥或完整请求正文。
- 修复 Kimi K3 测活通过但创作台首轮被误判失败：K3 现在保留官方支持的 `tool_choice=required`；仅对明确不支持该选项的 K2.7 Code/K2.6 及兼容别名使用 `auto` 配合首轮本地工具校验，未返回工具时不自动补发。
- 修复 Kimi K3-LS 网关别名在创作台拒绝 `tool_choice=required`：该未知兼容别名改用 `auto` 并保留本地首轮工具校验；官方 K3 在线工具编排保留 4096 token，K3-LS/已知兼容别名收窄为 2048 token，后台分镜首轮与授权修复仍使用 4096 token 并默认控制在 3–5 镜，降低慢模型在上游网关时限内触发 524 的概率。
- 修复 Kimi K3 首个带工具请求被严格 Schema 拒绝：画布对 Kimi/Moonshot Chat 工具显式发送 `strict:false`，保留可选字段与 metadata 扩展；其他 OpenAI 兼容网关不额外注入该字段，Responses/Gemini 仍按各自协议裁剪，普通文本测活不受影响。
- 缩短短剧入口的可用路径：用户输入“搭建短剧工作流”“生成镜头分镜”“拆成完整分镜”“创建影视项目”以及常见的“做/写/构思短剧”说法时，画布直接进入持久化影视会话，不再先发一轮在线 Agent 工具猜测请求；仍保留费用确认、幂等键、SSE 响应完整性和一次结构修复授权。
- 修复 Kimi 兼容别名在分镜首请求被 JSON Mode 扩展拒绝：官方 Kimi/Moonshot Chat 仍使用 `response_format: {"type":"json_object"}`，K3-LS、K2.7 Code/K2.6 等已知兼容别名改为只使用结构化提示词；普通 OpenAI 兼容网关不注入该供应商扩展，原有本地 JSON 校验和一次付费修复仍保留。
- 一句话影视 Agent 结果现在同时写回真正的结构化分镜脚本节点，直接支持分镜图批量生成和手动复制视频提示词；原有剧本、镜头配置与成片占位节点继续保留。
- 在线 Agent 的通用节点工具也允许创建 `script` 分镜节点，工具 Schema 与执行层保持一致，避免模型需要分镜脚本时被错误限制为普通文本节点。
- 影视 Agent 缺少文本模型配置时不再返回内置样例分镜伪装成功；后端会在供应商调用前明确失败并保留未调用边界，要求先配置当前文本模型，测活只作为管理员诊断提示。
- 修复图片编辑与 OpenAI 兼容视频的 multipart 请求体写入错误被静默忽略：字段序列化失败现在会在连接供应商前明确终止，不再把损坏正文发给上游。
- 修复在线 Agent 已解析配置被再次按裸模型名解析：多渠道共用同一模型时，创作台会继续使用测活时绑定的渠道、协议和端点，不再因第二次解析落到另一条渠道。
- 修复兼容网关使用无 `data` 的 `event: message_stop` 结束 Chat SSE：系统/自定义代理现在把该事件识别为合法终态，避免创作台工具调用明明已完整返回却被判为截断并进入费用待核对。
- 对齐后台 Worker 的 SSE 终态识别：无正文的 `event: message_stop`、`response.completed` 等合法结束事件不再让影视文本任务误报“缺少完成标记”。
- 修复 Kimi/兼容网关首轮工具成功、工具结果回传后第二轮被拒：Chat Completions 现在会补带对应工具名，并保留终态消息中的可见 `content`、`reasoning_content`、`toolCalls` 与增量推理片段，避免丢失多轮工具协议上下文。
- 修复 Gemini 兼容网关把同一无稳定 ID 的 functionCall 拆成多个 SSE chunk 时的重复执行：创作台现在按函数名和参数合并分片，保留同名并行调用，不再把同一工具重复写回画布。
- 兼容拒绝空 JSON Schema 必填列表的 OpenAI 网关：画布 Agent 工具没有必填参数时不再发送 `required: []`，有真实必填参数的工具约束保持不变。
- 加强测活与创作台绑定诊断：当前模型/协议没有精确测活时，页面会显示最近一次实际测活对象并提示风险；短 Agent 与长分镜都保留明确预算/费用确认，不能把另一模型或协议的成功结论误当作当前调用证明，但普通用户仍可明确尝试。
- 修复原生 Gemini 文本测活通过但创作台工具请求被 Schema 拒绝：Gemini `functionDeclarations` 现在使用独立的 OpenAPI 子集裁剪器，去除 OpenAI 专用或不受支持字段，同时保留数组、枚举、嵌套对象和必填字段。
- 对齐兼容网关的浏览器响应解析：Chat Completions 的普通与流式工具调用现在同时识别顶层和 `data.choices`，终态消息中的完整 `content` 与 `tool_calls` 也会合并，避免后台测活能读到文本而创作台把真实工具调用判成空结果或丢失第二轮上下文。
- 扩展兼容网关响应归一化：Responses/Gemini 流式事件支持 `data` 包装，Chat/Responses 工具参数也兼容对象形态，避免网关只改变外层结构后创作台丢失工具调用。
- 兼容旧式 Chat 工具响应：创作台现在识别 `function_call`、顶层 `name/arguments` 和 `call_id` 形态；完整终态缺少工具 ID 时补稳定本地 ID，避免测活能返回文本但兼容网关的工具调用被丢弃。
- 加固浏览器侧 SSE 完整性：Chat、Responses 与 Gemini 只有观察到完成标记、`[DONE]` 或等价终态事件才交付工具调用；自然 EOF、断流或半截参数会按费用状态不确定停止，不执行画布操作或自动发送下一轮。
- 修复浏览器流式解析把无正文 `response.incomplete` 当作普通完成：Chat、Responses、Gemini 现在保留明确的未完整状态，在线 Agent 不会把半截文本或工具参数交给画布。
- 补齐 Responses 流式工具事件：汇总 `response.output_item` 与 `function_call_arguments` 分片，即使最终完成事件只带元数据也不丢失工具调用；工具结果回传前会剥离 Kimi/Gemini 专用临时字段，避免跨协议污染下一轮请求。
- 修复后台工具诊断按 `item_id` 与 `call_id` 混用导致 Responses 参数分片被拆成无名调用：标准 `response.output_item.added`、`function_call_arguments.delta/done` 现在会合并为同一工具调用，并保留真实 `call_id`。
- 修复浏览器 Responses 流式工具调用覆盖真实 `call_id`：参数分片只带 `item_id` 时继续沿用首个事件的回传 ID，创作台第二轮工具结果不会误用内部 `fc_*` 项目标识。
- 收紧系统渠道中转地址识别：只有同源 `/api/ai/system/{channelId}` 路径才可绕过自定义渠道中转，包含相同路径片段的第三方 Base URL 不再有机会让浏览器直传用户 API Key。
- 收紧 Backend 旧任务配置的系统渠道推断：只接受服务端生成的相对代理路径，不再从任意绝对 URL 或中间路径片段提取 `channelId`，避免自定义请求误用系统渠道和平台积分。
- 加固 Responses 兼容流：终态 `output_text.done`/完成事件中的完整正文优先合并，且不会让早期 `{}` 占位参数覆盖后续函数参数分片，避免创作台拿到半截文本或损坏工具参数。
- 收紧 Kimi 等推理模型的工具确认恢复：只在内存中保留 `reasoning_content`，持久会话仅记录是否依赖该临时状态；刷新后不会省略上下文继续发起付费请求，而是明确停止并要求重新发送。
- 收紧在线 Agent 影视分镜费用边界：`canvas_create_cinematic_session` 在工具执行前单独确认一次结构修复授权，未确认不创建后台会话或访问供应商；授权随创建键持久化并在刷新/响应丢失恢复时保留，旧记录缺少授权时默认只允许首轮请求。
- 新增画布“手动交付”模式：短剧流程可生成并核对分镜图、视频提示词后停止，复制按钮输出可读的逐镜交付文本；该模式隐藏批量视频生成入口，避免慢模型、524 或费用不确定时误提交视频任务。零基础教程、短剧 Skill 和待验证清单同步说明外部网页工作台的人工提交边界。
- LLM 测活任务新增两层服务端幂等身份：Web 在 POST 前持久化随机提交键，同一用户即使响应丢失时探针已经终结也永久返回原任务；接管其他标签页或管理员的活动探针时，每个提交键都会另行绑定到原任务。相同自定义渠道配置在当前用户活动期内复用，同一系统渠道与模型再通过数据库部分唯一索引在所有管理员之间全局互斥。双击、刷新重发、多标签页或多实例竞态都会接管原任务并明确“未创建新的供应商调用”；系统探针可由其他管理员继续轮询，但不会开放跨用户自定义探针。收到明确响应后仍可主动重新测活。
- Windows 本地启动会解析并显示 Compose 实际项目名，首次健康启动后持久记录；仓库目录或 `COMPOSE_PROJECT_NAME` 意外变化时在创建容器前阻止切换到另一套数据卷。两种服务器安装器也会为全新实例固定合法项目标识。零基础与部署手册同时区分 Docker 内部 8080 和宿主机 Web 健康入口，减少把正常网络边界误判为后端宕机或重新注册到空数据库的风险。
- 视频失败任务的“手动查询”现在持久化供应商建议的下次轮询时间，暂态 408/429/5xx 与网络故障复用指数退避；数据库恢复租约会原子拒绝提前或并发查询并返回“本次未访问供应商”，管理员审计也移动到真正访问上游之前。任务详情同步显示下次可查询时间，避免连续点击轰击供应商或把查询请求误当成新的生成。
- 将管理员控制面的账号停用与会话撤销、公告、系统渠道/模型/价格、旧成本基准、邮件/Linux.do/注册/OSS、分镜模板、运行/积分策略、调账、兑换码和人工账单核对全部与审计事件收敛到同一数据库事务：审计失败会连同账号、余额/流水、订单处置和配置整体回滚，渠道与账单响应也不再在提交后补查而产生半成功。可用管理员按固定顺序加锁，降低并发操作同时移除最后管理员的风险；系统设置保存还会复核页面读取版本，防止空白密钥字段把另一位管理员刚更新的凭据覆盖回旧值。审计元数据不记录各类密钥、密码、Client Secret、Base URL、供应商正文或完整提示词。
- Web/Backend 新增面向付费长请求的优雅停机：内置 Nginx 以 SIGQUIT 保留正在代理的 SSE；Backend 收到 INT/TERM 后先停止 Worker 领取任务并让健康检查降级，再并行排空 HTTP/SSE 与已领取后台任务。所有 Compose 默认等待应用 40 分钟、容器 45 分钟后才强制结束，排空到期后还给同步请求最多 30 秒完成日志与账务收尾，避免部署沿用 Docker 短宽限直接切断慢模型。停机 duration、至少 1 分钟的容器收尾余量以及最长业务时限不超过排空窗口都会在启动/策略保存时强校验；安装配置、环境样例与管理手册同步说明调整顺序，旧 server Compose 的 CORS 通配默认值也改为安全同源空值。
- 加固管理员“请求明细”和运营分析 CSV：供应商错误、模型名、请求 ID 等外部单元格在 `= + - @`、前导控制/Unicode 格式字符或空白绕过时统一强制为文本，避免管理员用 Excel 打开审计文件时触发公式；规范负数仍保持数值。两类导出收敛到独立 CSV 文件并显式处理逐行写入与 Flush 错误，不再可能把部分审计内容当成成功下载。
- 新增 Linux PostgreSQL 部署安全备份脚本：要求管理员确认费用风险，并在关闭公开入口前后核对数据库活动任务及执行中系统计费订单，按顺序暂停 Web/Backend，记录原服务状态并在退出时恢复；转储会校验归档目录，Backend 数据以 archive 模式保留属主/权限并强制包含非链接 `.settings-key`，部署配置、版本、镜像摘要、文件元数据与全部文件 SHA-256 一并保存。并发、递归目标和已有恢复点会被拒绝，失败只留下明确的 `.incomplete` 现场；镜像安装器必须同步安装同发布版本的备份脚本，个人渠道人工核对、OSS 与 Redis 边界、校验和恢复流程已同步到管理手册。
- Windows 本地启动脚本新增 `CANVAS_HTTP_PORT` 环境变量与根目录 `.env` 解析、端口范围校验和监听进程诊断；外部程序占用端口时会在构建及容器变更前退出，已有项目 Web 容器仍可原地更新；Docker Engine 权限不足时会给出 `docker-users` 与重新打开 PowerShell 的操作提示。两套本地 Compose 统一按变量发布 Web 端口，健康检查、最终地址、失败日志和零基础/部署文档同步使用实际端口。
- 生产镜像部署的 Web 端口默认只绑定 `127.0.0.1`，安装器输出改为本机 Web 上游地址并明确要求 HTTPS 反向代理，避免把未加密入口直接暴露到公网或误把本机地址当成公网入口。
- 将后台任务供应商调用边界细分为 `pending → preflight → dispatched`：真正 HTTP 建连前必须在有效 Worker 租约下原子写入 `dispatched`，用户取消与发送请求竞争同一任务行。运行中但仍处于本地预检的任务现在可确认未调用并退款；已经发送、旧状态不明确或首轮完成后进入结构修复的任务继续进入费用待核对，且第二次调用准备不会抹掉首轮发送事实。Worker 崩溃与租约恢复也复用该边界，避免明明未建连却长期冻结积分，或已经调用后错误退款。
- 给所有出站地址预检增加可取消的 5 秒 DNS 截止时间，任务配置解析、参考 OSS 读取与 NewAPI 媒体 URL 校验也继承任务取消信号，修复调用前阶段绕过总时限或因错误正文含“超时”误进待核对的问题。当前请求未建连时会同时核对该任务更早的系统或自定义渠道成功调用，再分别显示退款或原费用待核对，不再把第二次结构修复失败误写成整单退款；HTTP 2xx 已记入成功调用但后续 JSON/业务处理失败时，任务正文也会补上费用待核对和禁止立即重试。模型目录同步增加 8 MiB 响应、5000 原始条目、2000 有效模型与字段长度上限，并以 100 条分批写库，避免异常供应商目录放大内存和数据库参数压力。
- 将系统渠道模型表确立为启停、协议与定价的唯一事实源：渠道新增/修改、模型启停/删除、兼容清单和测活失效改为按渠道串行的事务写入，启动时自动修复旧清单漂移且不会复活软删除模型；后台新任务和同步代理不再依赖可能过期的 `models_json` 授权。较慢的模型目录拉取若遇到渠道地址、密钥或协议被修改，会放弃旧目录而不是误挂到新配置。
- 加固供应商成功后的媒体交付：图片、视频和音频按“全批解码与额度预留、任务/计费尝试/结果路径幂等落盘”执行；批量中途写入失败也会保存已完成资源、未完成内联正文和 pending 对象检查点，“恢复已保存结果”只续写原对象并重放本地交付，不再请求供应商。同一次恢复复用摘要一致的 ready 资源，新付费重试保持隔离，failed/pending 行不再长期占用 ready 文件配额；长恢复持续续租并在对象间核对，任务列表只投影恢复标记而不传输超过 1 MiB 的 Base64 检查点。会话、画布或任务终态事务失败同样可从任务页恢复，后端同时阻止付费重试。
- 修复成功任务与项目工作流之间的交付断点：任务产物登记现在能识别业务项目及其关联画布的真实归属，工作流链接、步骤推进、产物表示和修订号保持同一事务；重复或多实例并发登记只提交一次，不再重复增加项目/工作流版本。任务结果已持久化但后续登记中断时，重新打开项目会限量自动补登记并留下任务恢复日志。
- 加固付费任务的 JSON 序列化边界：OpenAI/Gemini 普通、流式及媒体请求体只有在序列化成功后才允许建连，失败会明确标记未调用供应商并退款；会话快照和任务输入必须在创建会话、任务与计费单前成功保存。供应商已成功后的任务结果或画布操作若无法序列化，不再以空结果完成并结算，而是原子写入失败终态、将费用转为待核对并禁止立即重试；Agent 会话终态写入错误也不再静默吞掉。
- 补齐 Gemini GenerateContent 原生文本协议：后台测活、普通文本与长分镜统一使用 `streamGenerateContent?alt=sse`、`x-goog-api-key`、256 token 探针上限和渐进 SSE 诊断，长分镜额外请求 JSON MIME，思考片段与截断正文不会混入分镜解析，失败后也不会补发第二笔 `generateContent`。系统同步代理会为官方根地址补齐 `/v1` 或 `/v1beta`，并从 Gemini URL 路径提取模型名用于授权、计费、日志和真实流式观察，修复根地址或 Gemini 系统渠道在真正调用前失败的问题。
- LLM 测活从“正文是合法 SSE”升级为“实际观察到渐进分片”：后端记录供应商首字节、首尾正文到达间隔、读取批次、后续读取累计等待和最长单次等待；只有后续上游读取本身累计发生可观测等待才会染绿，本地转发背压不会误判。完整但单包或疑似被网关缓冲的 SSE 标记为未证实并在费用确认中提示长请求风险，系统在线 Agent 的真实流式观察采用同一口径；旧版测活结论随校验版本升级失效，但这些诊断状态不再阻止普通用户调用。Kimi K3 长分镜同时显式使用 low 推理强度，降低默认最高推理档造成的首包等待。
- LLM 测活对 Responses 与 Chat Completions 分别设置 256 token 输出硬上限，在线画布 Agent 的普通模型每轮工具编排限制为 2048 token，官方 Kimi K3 使用当前 `max_completion_tokens` 字段、low 推理强度和 4096 token 有界预算，K3-LS/已知兼容别名收窄为 2048 token，降低非流式模型把小请求扩写成数千 token 并在约 5 分钟触发 524 的概率。Chat 工具循环会在内存中保留并回传 Kimi 所需的 `reasoning_content`，但不展示或写入会话诊断；完整结束但语义为空、首轮忽略 required 工具调用、指定工具不匹配或明确达到输出上限时会计入真实调用并停止，不执行画布操作、使用半截参数、复用旧工具结果或自动发送下一轮。
- 渠道文本测活通过后新增可选的“测试工具调用”：由后台持久任务使用固定 `probe_extract_record` 无副作用工具、随机校验码和 512 token 硬上限，直接复用创作台的渠道/协议/中转路径，区分“文本可用”和“Function Calling 可用”，不执行画布写操作或接受任意测活提示词；响应丢失时可恢复原任务，不会让浏览器重发不确定请求。
- Worker 首轮生成与已授权分镜修复新增持久化原子供应商调用检查点：只有未过期的当前任务租约、调用阶段和调用前审计在同一事务中成功后才进入 `preflight`，随后还要在 HTTP 建连前写入 `dispatched`；租约交接或日志写入失败不再被忽略并继续产生孤儿或重复调用。无订单任务只有明确 `pending/preflight` 才允许安全接管，旧空值保持保守；任务付费重试也将重新入队、新计费预留、Agent 会话恢复和用户确认日志收敛到同一事务，避免半成功重试。
- 后端任务日志、资源失败状态、项目创建回滚、活动统计和过期会话清理不再静默吞掉持久化错误；底层数据库、存储和结算原因只写服务端日志，任务页改用稳定核对提示。
- 统一任务失败、计费备注与请求明细的公开错误边界：供应商明确拒绝只提取结构化短错误码和消息，完整响应正文、数据库/Redis 诊断、网络 URL、文件路径及密钥片段只留 Backend 日志；408、499 与除 501 外的 5xx 继续保留费用待核对和禁止立即重试语义，上游成功但本地结果持久化失败会明确引导恢复而不伪装成普通生成失败。
- 长分镜保留真实 SSE 请求与响应完整性校验，但取消“测活通过才可调用”的硬门禁：系统与自定义渠道的未测活、过期、非流式或失败状态只在费用确认中提示风险，普通用户仍可明确确认后创建任务；系统渠道继续校验模型目录和用户积分价格，自定义渠道继续冻结当前配置，真实长任务观察到非流式、524、连接中断或半截结果后按费用待核对并禁止自动重放。影视 Agent 移除前端四分钟假超时并支持暂态查询退避，创建意图会在请求前落盘并跨刷新复用原键，中断窗口可补齐，失败/取消任务与会话终态改为原子同步并可查询自愈；画布及会话活动任务由数据库唯一约束复用原任务与订单。
- 补齐“零基础快速开始 + 完整电商案例 + 管理部署手册”的统一入口，校正文档中的 GitHub Raw、源码仓库和镜像 Compose 下载示例，并补充 Docker Desktop Engine 未启动与恢复演练费用边界。
- 部署健康检查从进程存活升级为数据库、运行时协调与 Worker 调度联合就绪检查，异常返回 503；新增 Windows 本地启动脚本，在构建前识别 Docker CLI、Engine 和 Compose 故障并输出组件状态。
- 多实例 Worker 的固定 45 分钟全局槽位改为短租约持续续租，Redis Key TTL 不再被较短请求反向缩短；槽位失效会暂停新任务并降级健康状态但不粗暴取消可能计费的当前请求。Backend 重启或任务租约中断后，仅 reserved 订单和已有任务 ID 的异步视频可自动继续，其余请求终止并禁止供应商重放；单任务 panic 被隔离并按调用边界退款或转待核对。
- Backend 统一生成请求编号并收敛内部错误出口：未知数据库、Redis、存储、解密、OAuth 与 panic 故障只在服务端关联日志，公共响应不再暴露路径、主机或底层错误；访问日志移除查询参数和请求头并脱敏公开分享 Token，同时保留明确业务错误和供应商计费风险提示。
- 在线画布 Agent 新增每条消息 1/2/4 次文本推理预算、持久调用进度、工具确认互斥及明确停止/离开保护；系统与自定义渠道文本请求改为逐块透传 SSE，并能识别误标 Content-Type；后台 Worker 的测活、分镜和影视任务同步校验事件 JSON 与完成标记。损坏事件、缺少完成标记、用户停止、504/524、连接中断和日志落库失败均停止后续调用，半截结果不再伪装成功；系统渠道真实调用会以请求时配置指纹更新流式门禁，迟到旧请求不能覆盖新测活结论。
- 在线 Agent 的每条用户消息与每个模型轮次改用稳定系统渠道幂等键；服务端要求付费 POST 携带合法键，并在等待渠道并发前拒绝已经存在的键，写入竞态继续由账务唯一索引兜底。重复请求统一返回 409 并明确未再次调用供应商，不缓存旧响应，也不会因响应丢失自动换键重试。
- 统一上游失败的计费边界：已收到 408、499 与除 501 外的 5xx、连接中断及响应超限均按费用不确定处理，系统渠道不再把 500/502/503 自动退款，自定义渠道和图片、视频、音频前端同步显示禁止立即重试的核账提示；约 5 分钟且供应商标记为非流式的 524 会明确提示属于上游/CDN 固定等待上限，不能靠调大本地超时修复；调用供应商前的本地 504 仍保留明确无费用口径，400/401/403/404/422/429 与接口未实现继续作为明确拒绝处理。
- 加固“请求日志优先于结算”的账务边界：上游成功但 API 日志或积分终态写入失败时，非流式与 SSE 都阻止 Agent 把结果作为成功继续调用，并留下任务与进程日志；待核对订单不能再被普通 Worker 或同步代理自动结算或退款，只能由管理员核账或明确的原上游任务恢复路径处理。
- 统一后台任务执行超时口径：图片、音频、视频和未知任务不再提示直接重试，全部按可能仍执行和费用待核对处理；视频已有上游任务 ID 时引导手动查询，渠道并发槽位等待超时则明确未调用供应商并按退款处理。前端费用风险识别优先尊重“未调用”证明，任务终态后的退款或待核对状态写入失败会留下错误日志。
- 系统与自定义同步模型中转统一服从后台超时策略；系统渠道把等待并发槽位计入总时限并用同一时限维护槽位 TTL，等待阶段超时明确为“未调用、未计费”，请求发出后的超时继续按费用待核对处理。Backend 仅限制慢请求头与空闲 Keep-Alive，不设置会截断长上传或 SSE 的全局读写超时。
- 自定义渠道在收到上游 2xx 后若发生内容类型不匹配、JSON 无效/超限或 SSE 损坏、中断、缺少完成标记，不再降级成普通可重试 502：POST 保留可能计费、禁止立即重试及请求编号，前端不再用无编号通用 5xx 文案覆盖；模型目录 GET 保持非计费配置错误，流式底层诊断写 Backend 日志且不回传响应正文。
- 本地 Canvas Agent 连接改为先确认初始画布状态同步成功再开放发送；后续快照串行写入并校验超时与 HTTP 响应，每轮发送前强制核对最新状态，画布写工具先同步新快照再回传完成，状态或工具结果通信失败时明确断开，避免 Agent 基于旧画布继续操作；移除可由旧偏好重新显示、但只有“暂未实现”弹窗的 AI 超分入口，保留真实可用的图片放大功能。
- 本地 Canvas Agent 启动凭据改用不会发送给 Web 服务器的 URL fragment，读取后立即清除且 Token 仅保存于浏览器会话；网页只允许本机回环地址，HTTP 与 SSE 统一使用 `X-Canvas-Agent-Token`，本地服务拒绝查询参数 Token，旧链接只做清除告警。自动连接不再关闭工具写操作确认，未登录跳转、画布导航与诊断日志也不会继续携带凭据。
- 个人自定义渠道 API Key 从普通 Zustand 配置快照拆出：新渠道默认只保存到账号级标签页会话，用户可在可信设备明确选择持久化；退出或切换账号清除会话密钥，旧版明文快照会无损迁移并立即脱敏，配置仓库改为服务端确认身份后才恢复，画布、导出与云同步继续不携带密钥。
- 生产 Web 新增兼容画布 Worker/WASM、OSS 媒体、本地 Agent 与 SSE 的 CSP，以及 MIME、点击劫持、Referrer、浏览器权限和按 HTTPS 条件发送的 HSTS；首页主题初始化移除内联脚本，Backend 直连响应使用更严格的纯 API 安全策略。
- 收紧 Cookie 登录跨域边界：Backend 启动时拒绝通配符和非精确来源，同源放行同时校验协议与原始 Host，不再信任转发 Host 或任意 localhost 端口，并阻止 Fetch Metadata 明确标记的无 Origin 跨站写请求；退出时即使服务端撤销失败也先让本机 Cookie 过期，再返回可处置的错误提示。
- 登录会话创建改为原子清理过期记录并为每账号保留最近 20 个，侧栏支持一键撤销当前浏览器以外的全部会话；管理员修改密码、角色或账号状态会强制撤销旧登录，数据库撤销失败时不再静默保存安全变更。
- 修正登录账号频控被 IP 分片绕过的问题：账号窗口改为跨 IP、跨实例累计且协调键只保存摘要；单机 SQLite 协调器定期回收过期限流键和空并发 scope，限流响应按真实窗口返回 `Retry-After`。
- 加固注册邮件与验证码：新码先以 pending 状态原子替换旧码，只有 SMTP 成功并写入发送时间后才可验证；错误五次跨实例锁定，最终注册再次核对邮箱、用途、发送态与次数，收件人按摘要限流。SMTP 认证禁止明文传输且完整会话有 deadline，公共失败响应不再泄露内部邮件主机或底层错误。
- 新增自定义渠道与系统文本模型测活：以后台任务执行带随机校验码的真实信息抽取，记录耗时和调用日志；系统模型持久化带配置指纹的流式诊断，个人模型在浏览器保存不含密钥的小型结论及测活任务 ID，服务端据此提示长任务配置风险且随凭据变更失效，慢探针状态轮询不会在约 4 分钟时自触发限流；在线 Agent 显示非流式 524 风险，影视项目仍可在费用确认后尝试；后端文本生成优先消费 SSE，只有上游明确拒绝流式时才回退非流式；慢推理默认文本时限提高到 15 分钟，分镜总时限提高到 30 分钟。
- 生成任务页面不再用固定 12/13 分钟抢先判定文本或分镜超时，改为低频跟踪后台权威终态；任务中心、画布单节点、批量镜头与脚本分镜重试统一先核对原任务终态和计费状态，再由用户确认创建新的供应商请求，服务端同时校验来源任务并记录确认事实；排队与运行中取消改为分级风险确认，运行中请求持久化“供应商可能继续执行、费用待核对”说明，退款或风险说明保存异常不再把已生效的取消误报为失败。
- 画布“停止生成”改为等待并回填后台取消终态，确认期间保持生成入口锁定；取消控制请求设置 30 秒时限，响应丢失时读回原任务确认是否生效，无法确认则保留费用风险且禁止伪装成可重试；刷新后仍绑定排队/运行任务的节点继续禁用生成；单项显式取消只停止对应任务轮询，不再重复调用取消接口或连带中断同批其他任务。
- 下线 Electron Desktop、网页账号池和 noVNC 桥接，视频生成统一收敛到已配置模型渠道与后端持久任务队列，部署只发布 Web 和 Backend 镜像。
- 删除已无调用方的浏览器图片、音频和视频生成/轮询客户端及固定前端超时；媒体请求只能通过后端持久化任务创建，在线 Agent 文本和模型目录继续使用受限同源中继，避免第三方 CORS、浏览器密钥直传和绕过任务幂等与计费恢复。
- 同步上游至 `v1.0.23` 的关键修复：视频 Composer 保留结构化首尾帧并按协议排序，图片批次主图完整同步媒体元数据并显示明确选中态。
- 异步视频任务改为可跨进程恢复的持久化轮询，覆盖所有已支持协议、暂态退避、总超时、当前尝试上游任务 ID 恢复及管理员手动查询。
- 加固任务终态与计费并发控制，避免取消/完成互相覆盖、重复结算退款、历史重试订单误关联及恢复任务重复创建上游请求。
- 优化任务与 API 日志高频查询、稳定分页和 PostgreSQL 索引，并以按用户锁替代全局存储配额锁，降低不同用户间的无关阻塞。
- 派生并品牌化为“明想 MingWant Studio”，保留上游完整无限画布、节点连线、生成、短剧、Agent 与管理后台能力。
- 新增 5 条真人素材到 15 条约 30 秒成片的电商画布模板，包含创意矩阵、结构化分镜、首尾帧策略、质量门禁与交付清单。
- 新增 `commerce-video` 项目类型和后端九步电商生产工作流，短剧项目继续使用原标准流程。
- 接入 49 份自有提示词组成“明想提示词库”，支持分类检索、原文预览、复制及从画布文本节点按需插入。
- 优化提示词库工具栏、带货模板首屏与复制反馈，并增强画布对异常技能和任务数据的容错。
- 加固短剧分镜结构化输出：网页与 Agent 共用 JSON 规范化；可能产生第二笔供应商调用的结构修复改为用户事前授权且最多一次，并在请求日志和任务结果中留痕；项目镜头改为 Agent 分批直接提交结构化工具参数并返回精确字段错误。
- 将参考爆款视频教程整理为供应商无关的合规制作流程，不纳入第三方去水印步骤。
- 部署镜像、容器、数据库默认名与界面品牌调整为 MingWant Studio，并保留必要的内部兼容标识。

## v1.0.9

- 修复 Seedance `/videos` 请求遗漏声音生成参数的问题，启用声音时生成结果会包含上游音轨。
- tldraw License Key 改由前端镜像构建参数注入，服务器部署可通过环境配置更新授权。

## v1.0.8

- 项目画风扩展为 18 套预设，支持查看完整规范，并在生成角色三视图时引用当前已保存画风。

## v1.0.7

- README 增加临时演示环境入口与测试账号说明，便于外部演示与验收。

## v1.0.6

- 账号活动任务上限统一跟随后台热配置，任务中心默认展示全部任务，并在创建或重试后自动切换到运行中记录。
- 修复分镜脚本失败重试误调用图片模型的问题，支持从文本节点连线创建分镜脚本并继承剧情内容。
- 视频节点角色引用改用稳定节点标识，角色三视图、视觉设定、声音描述及渠道支持的声音样本可随视频任务和重试共同提交。
- 角色引用架与行内标签优先预览人物三视图，避免显示文档或文字占位图标。

## v1.0.5

- 新增短剧项目空间与结构化资产创作闭环，支持项目、章节、角色、镜头和画布资产协同管理。
- 管理员可从上游渠道一键拉取模型目录；缺失模型自动导入为停用待定价，避免浏览器直连上游时的 CORS 问题。
- 管理后台新增统一资源与请求策略配置，支持默认重置、自用模式、即时生效和用户配额用量查看。
- 画布媒体支持复制私有 OSS 签名地址，图片生成支持透明背景与 21:9 尺寸。
- 修复音视频持久化恢复与短剧项目空间合并引发的 Web 构建错误。
- 镜像安装部署可显式复用既有后端、PostgreSQL 和 Redis 数据目录。

## v1.0.4

- 系统后台支持热更新 Worker 与全局渠道并发，各系统渠道可跟随全局值或单独覆盖；渠道满载时请求等待空闲槽位，不再直接失败。
- 加宽画布图片、视频模型选择器及弹出菜单，减少长模型名称和说明被截断。

## v1.0.3

- 系统公告正文支持受控 HTML，用户可以看到蓝色链接并在新窗口打开，同时支持粗体、斜体、下划线、列表和换行等常见格式。
- 修复本地 Codex 会话新建后首次发送失败的问题。

## v1.0.2

- 增加前端与后端多架构 GHCR 镜像发布，并让服务器一键部署直接拉取指定版本镜像。
- 完善 Codex App 插件的仓库 marketplace 安装入口。
- 修复 xAI 视频生成请求未使用官方字段的问题。

## v1.0.1

- 自定义模型渠道改由登录态后端中转，减少第三方接口 CORS 限制。
- 增加 xAI 与 Sub2API 的 Grok 视频生成协议支持。
- 增加私有上游精确主机白名单，收紧自定义渠道访问边界。
- 修复 Agent 会话未展示后端具体失败原因的问题。
- 修复主页删除最近项目时未显示确认弹窗的问题。
- 修复 PostgreSQL 用户活跃统计冲突更新时的列歧义问题。
