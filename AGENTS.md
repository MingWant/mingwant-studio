# AGENTS.md

本文件约束本项目中的 AI 和自动化开发行为。先遵循用户当前任务，再遵循本文件。

## 工作方式

- 先读代码、配置和文档，再判断和修改；结论必须有代码或命令输出依据。
- 默认推进到可交付状态，但只改任务相关文件，不回滚或覆盖已有用户修改。
- 优先小而完整的实现，不为假想兼容场景增加分支，不引入无实际收益的抽象。
- 项目尚未上线；除非用户要求，不为旧字段和旧数据编写迁移兼容层。
- 本工作区本地启动后端时必须复用 `.local/project-workbench-debug`，通过 `CANVAS_BACKEND_DATA_DIR` 显式指定；启动前先确认该目录中的现有数据库，禁止直接使用 `backend/data` 创建或切换到另一套本地账号数据。
- 本地 Compose 启动必须保持原 `COMPOSE_PROJECT_NAME`；Windows 预检脚本首次健康启动后用 `.local/compose-project-name` 记录实际标识，后续标识变化必须在 `compose up` 前停止，不得通过自动删除标识、迁移或新建卷掩盖已有账号数据。
- 按用户要求，写完代码不执行语法检查、测试或构建；交付时明确说明未运行验证。
- 反复出现的项目约束应补充到本文件，规则要具体、可执行。

## 代码质量

- 页面或服务只保留本层职责。纯算法、协议转换和可独立测试的业务规则放到对应 `lib` 或 service 文件。
- 不新增只改名或透传 props 的组件，不用 helper 掩盖超长主流程；先拆清职责边界。
- 核心业务入口、非直观算法、安全边界和降级策略必须有简短中文注释，说明“为什么”和关键约束；显而易见的赋值不写注释。
- 新文件建议控制在 500 行以内；文件超过 800 行时，新增功能前应优先拆出明确职责。历史超长文件按功能逐步治理，不做无关的大爆炸重构。
- 错误不可静默吞掉。读展示路径允许降级并提示，保存、生成、权限、删除等写路径必须明确失败。
- 通用格式、解析、压缩、加密、日期等能力使用成熟库，不手写底层实现。

## 后端

- 技术栈：Go、Gin、GORM、SQLite。
- `handler/` 只处理 HTTP 入参、调用 service、返回 `OK` / `Fail`。
- `service/` 负责业务逻辑、校验、鉴权、默认值、时间和 ID。
- `repository/` 只负责数据库访问和 GORM 查询；`model/` 只定义结构、枚举和简单模型方法。
- 列表接口沿用 `model.Query`、`Normalize`、分页和标签筛选；响应保持 `{ code, data, msg }`。
- 新增或调整数据表时同步更新 `docs/content/docs/backend/backend-database.mdx`。
- 生成、激活、权限、删除和密钥处理属于强校验写路径，不允许用默认值掩盖失败。
- 供应商 JSON 请求体必须在建连前成功序列化，序列化失败要明确记录“未调用供应商”并退款，不得发送空正文；任务输入必须在创建任务和计费单前成功序列化。供应商已成功后的任务结果与画布操作若无法序列化或持久化，任务必须失败、费用进入待核对且禁止立即重试，不能保存空结果后继续结算。
- 供应商成功后的内联媒体必须先完成全批解码与额度预留，再按“任务、当前计费尝试、结果路径”幂等写入资源；同一尝试恢复时复用摘要一致的 ready 资源，新的付费重试不得复用旧尝试结果。任一对象写入中断时必须保留已完成资源引用、未完成内联正文和 pending 对象身份的本地检查点，“恢复已保存结果”只续写本地对象，不得再次调用供应商；长恢复必须续期任务租约并在对象之间核对，租约失效后不得继续写下一个对象。媒体落盘后仍要先保存任务结果与画布操作检查点，再提交会话、画布和任务终态事务。failed/pending 资源不得占用 ready 文件总配额。
- 成功任务登记项目工作流产物时，任务可能直接属于业务项目，也可能属于该项目关联画布；后端必须核对真实归属，不能只相信客户端提交的项目 ID。任务、步骤和产物链接必须在同一事务中按任务与步骤唯一键幂等提交，重复登记不得再次推进步骤或增加项目/工作流修订号；任务结果已保存但登记中断时，项目详情读取允许限量补登记并记录恢复日志。
- 未知数据库、Redis、存储、解密、配置错误和 panic 只允许在服务端按服务端生成的请求编号与路由模板记录；公共响应使用稳定提示和同一请求编号，不得回传 DSN、路径、主机、底层连接错误或密钥。访问日志不得记录查询参数或请求头；明确的 4xx 业务错误、经过审查的 5xx 可操作提示和供应商计费风险文案可保留。
- `Task.Error`、普通用户可见任务日志、计费备注和后台请求明细只能保存经过审查的业务语义。供应商 4xx 最多提取结构化短错误码和消息，禁止保存完整响应正文；408、499、除 501 外的 5xx 只保留费用待核对口径。网络 URL、数据库/Redis 错误、文件路径、密钥片段和结算底层原因必须写 Backend 日志，不能拼进展示字段。
- Cookie 登录接口的 CORS 只允许同协议同 Host 或启动时校验过的精确来源，禁止 `*`、任意 localhost 端口和客户端可伪造的 `X-Forwarded-Host` 放行；无 `Origin` 且 Fetch Metadata 明确为跨站的写请求必须拒绝。反向代理必须保留原始 `Host` 并覆盖 `X-Forwarded-Proto`。
- 认证会话只保存 Token 哈希，创建时必须清理过期记录并限制每个账号的活动会话数量；用户应能撤销当前会话以外的全部登录。管理员修改密码、角色或账号状态时必须先成功撤销旧会话，撤销失败不得静默保存安全变更。
- 登录限流必须同时有独立的 IP 窗口和跨 IP 账号窗口，账号标识只以不可逆摘要进入 Redis/本机协调键；PostgreSQL 多实例使用 Redis 统一计数，SQLite 本机协调器必须定期回收过期窗口和空并发槽位，`Retry-After` 与实际限制窗口一致。
- 邮箱验证码只有 SMTP 完成且数据库写入 `sent_at` 后才可验证；新码必须原子失效旧码，错误次数跨实例落库并设置上限，消费时再次核对邮箱、用途、发送态、到期和次数。收件人频控键只保存摘要；SMTP 认证禁止明文传输并为完整会话设置 deadline，公共错误不得暴露主机、IP 或底层网络信息。
- 已收到上游 408、499 或除 501 外的 5xx，连接中断、响应超限、已成功返回状态后的协议/读取/解析失败都可能已经计费；系统订单必须进入待核对，前端必须提示不得立即重试。自定义渠道即使没有平台计费订单，也不得把 POST 的 2xx 后内容类型错误、JSON 无效/超限或 SSE 不完整降级成普通可重试 502；模型目录 GET 才使用非计费配置错误口径。400/401/403/404/422/429 等明确拒绝可退款；501 只用于“接口未实现”的安全兼容分支。只有上游以 400/422 明确拒绝 `stream` 参数时，才允许回退一次非流式。
- 系统与自定义渠道代理收到 SSE 时必须逐块转发并禁用代理缓冲；不能只信上游 `Content-Type`，请求明确要求流式时应以极短前缀识别误标 SSE。后台 Worker 直接消费 SSE 时也必须执行同样的事件 JSON 与完成标记校验。浏览器断开、事件损坏、缺少完成标记、流读取中断、响应超限、504/524 或成功请求日志写入失败时，必须阻止 Agent 使用半截结果，系统计费进入待核对；分镜不得拿半截正文进入 JSON 修复。正常成功必须先写 API 日志，再结算积分；订单一旦进入待核对，普通执行路径不得自动转为已结算或已退款，只允许管理员核账或明确的上游任务恢复路径处理。
- Gemini 原生文本必须使用模型协议 `gemini-content`、`x-goog-api-key` 和 `streamGenerateContent?alt=sse`；官方根地址由服务端补 `/v1beta`，已显式填写 `/v1` 或 `/v1beta` 时不得重复拼接。模型名来自 URL 路径而非请求体，必须用于授权、计费、日志和流式状态指纹。后台任务收到完整 JSON 可记录非流式风险，但失败或截断后不得自动补发 `generateContent`；分镜请求应使用原生 JSON MIME，Gemini thought 片段不得进入可见正文或结构解析。
- 真实系统渠道流式观察必须绑定请求开始时的配置指纹；管理员换密钥、协议或模型配置后，迟到旧请求不得覆盖更新的测活结论。
- `channel_models` 是系统渠道模型启停、协议和定价的唯一事实源；`model_channels.models_json` 只作兼容投影。渠道与模型的新增、启停、删除、兼容投影及相关测活失效必须在同一事务中提交，并按渠道串行化；启动时允许从完全没有模型历史的旧库迁移一次，但不得从旧 JSON 复活已停用或软删除模型。慢速目录拉取写入前必须确认渠道地址、密钥和协议没有在途中变化。
- 管理员账号与会话、公告、系统渠道与模型、成本/售价、邮件/OAuth/注册/OSS、分镜模板、运行/积分策略、调账、兑换码和账单核对等控制面强写入，必须把业务变更与审计事件放进同一数据库事务；审计序列化或落库失败时整体回滚，不能出现“操作已经生效但接口报错”。只有访问供应商等不可回滚外部动作允许先记录操作意图再调用。审计元数据禁止包含 API Key、访问密钥、密码、Client Secret、Base URL、供应商正文或完整提示词。
- 邮件、OAuth、OSS 等用空白密钥表示“保留原凭据”的全量设置写入，必须在事务内锁定并复核读取版本；检测到其他管理员已经修改、创建或重置时返回 409，不得用旧快照覆盖新凭据。
- 所有出站 URL 的建连前 DNS 预检必须继承调用上下文并设置最多 5 秒的独立截止时间；任务配置解析、参考资源读取、请求序列化及媒体 URL 校验等已知调用前阶段也必须保留“当前供应商请求未发出”证据。退款前仍要查询同一任务更早的成功计费调用，包括没有平台订单的自定义渠道；当前请求未发出但首轮已经调用时必须提示核对原费用，不能承诺整单退款。重定向、响应读取等建连后错误仍按费用不确定处理。模型目录响应最多 8 MiB、原始条目最多 5000、去重后最多 2000；模型标识和显示名分别不超过数据库的 120/160 字符边界，目录落库必须分批执行。
- `/api/health` 必须同时检查数据库、Redis/运行时协调器与 Worker 调度心跳；关键组件异常时返回 HTTP 503，响应只给组件级诊断，不暴露 DSN、Redis 地址或底层连接错误。
- 多实例 Worker 的全局并发槽位必须使用短 TTL 持续续租，Redis Key TTL 只能延长不能被短请求缩短；首次续租异常就必须暂停该实例领取新任务、让健康检查失败并留下服务端与任务日志，租约失效时不得取消可能已计费的当前任务，任务结束后再恢复。
- 过期数据库租约重新领取 running 任务时，只有计费仍为 `reserved`、新调用状态明确为 `pending/preflight`，或已保存可查询供应商任务 ID 的异步视频允许自动继续；其他文本、图片、音频和无任务 ID 请求必须终止为待核对并禁止自动重放。单任务 panic 必须被 Worker 边界隔离，不能击穿 Backend 进程；`reserved` 或能证明未 `dispatched` 的 `running` 订单退款，已经 `dispatched` 或 `uncertain` 的订单进入待核对。
- Worker 每次准备供应商调用时都必须在同一数据库事务中确认未过期的当前任务租约、把新尝试从 `pending` 更新为 `preflight`、更新调用阶段并写入调用前审计；真正执行 HTTP 请求前还必须原子更新为 `dispatched`，更新失败不得建连。运行中取消只有在同一任务行仍为 `pending/preflight` 且取消事务先赢时才能按未调用退款；`dispatched`、旧空值和旧版 `prepared` 一律按可能已调用处理。分镜结构修复等第二次调用必须再次续租和写审计，但不得把首轮 `dispatched` 覆盖回 `preflight`。崩溃恢复只有持久状态明确为 `pending/preflight`，或订单仍为 `reserved`，才允许自动继续；其他状态不能自动重放。
- 任务重试会创建新的供应商请求，后端必须要求显式费用确认并记录确认事实；原任务重新入队、新计费预留、关联 Agent 会话恢复和确认日志必须在同一事务中提交，任一步失败整体回滚。前端弹窗不能作为可绕过的唯一安全边界，重试也必须计入任务创建限流。
- 系统渠道付费 POST 必须携带能标识一次逻辑供应商调用的稳定幂等键；同一键应在等待渠道槽位前返回 409，写入竞态由用户与键的数据库唯一约束兜底，且不得再次调用供应商。幂等保护不缓存或重放模型响应，原调用响应丢失、执行中、已结算、已退款或费用待核对时都不能通过复用同键取得结果，也不得自动换键重试。
- 分镜结构自动修复会创建第二次文本模型请求；后端只有在任务输入显式携带 `allowPaidStructureRepair: true` 时才允许修复一次，并在任务日志、API 请求类型和任务结果中记录，连续无效时不得继续发起第三次请求。524、连接中断等结果不确定错误不得进入结构修复。
- 后台任务超时必须区分“供应商调用前”和“请求已可能发出”：等待渠道并发槽位超时要明确未调用供应商并退款；请求发出后的文本、图片、视频、音频及未知任务超时都必须提示费用待核对、禁止立即重试。视频已有上游任务 ID 时优先引导手动查询，不能用“稍后重试”覆盖恢复入口；任何终态后的结算、退款或待核对状态更新失败都必须写任务日志，不得静默吞掉。
- 视频人工查询只能轮询已经持久化的上游任务 ID，不得重新进入创建接口。供应商返回处理中或暂态查询失败时，必须把下一次允许查询时间写回任务；用户与管理员入口都要在数据库恢复租约抢占时复核该时间，提前请求返回 429 并明确未访问供应商，不能只靠前端按钮节流。
- Web 停机必须让 Nginx 以 SIGQUIT 排空代理连接；Backend 收到 INT/TERM 后必须先停止 Worker 领取新任务，再并行排空 HTTP/SSE 与已领取任务。容器强制停止宽限必须至少比应用排空窗口多 1 分钟，应用窗口必须覆盖最长同步中转和后台任务时限，启动与热更新保存都要强校验；强制断开同步请求后还要为请求日志、任务终态与费用待核对写入保留短收尾窗口。不得用 `kill -9`、过短 Compose stop timeout 或部署重启直接切断可能计费的请求；超过窗口的中断仍按持久供应商调用状态防重放并进入退款、原任务查询或待核对边界。
- 管理员 CSV 导出中的模型、供应商任务 ID、错误等外部文本必须防止表格公式执行：`= + - @`、前导控制/格式字符及空白绕过统一按文本转义，规范的负数值可以保留数值语义；所有记录写入与最终 Flush 错误必须显式返回，不能交付半截审计文件。

## 前端

- 技术栈：Vite、React、React Router、TypeScript、Ant Design、Tailwind、Zustand、TanStack Query。
- Ant Design 以当前安装版本和官方 `llms-full.txt` 为准；共性主题、按钮、下拉框、弹窗和反馈配置集中在 `app-theme.ts` 或 `AppProviders`。
- API 放在 `web/src/services/api/`；跨页面状态放在 `web/src/stores/`；路由页面、布局和路由配置分别放在 `pages/`、`layouts/`、`router.tsx`。
- 画布组件、状态和工具分别放在 `components/canvas/`、`stores/canvas/`、`lib/canvas/`。
- 页面私有组件和 hook 放在页面目录内；只有真实跨页面复用的能力才上移到全局目录。
- 已在全局 store 或 hook 中的状态直接读取，不通过多层 props 透传。复制、下载、确认等跨页面 UI 副作用优先收敛为全局 hook。
- UI 图标优先使用 `lucide-react` 或项目已有 Ant Design 图标；页面文案使用中文。
- 页面身份图标统一使用 `WorkspaceSignalIcon`，导航与操作图标统一使用 `lucide-react`；同一角色不得并行创造另一套图标容器或视觉语言。
- 页面级分类导航统一使用至少 44px 高的“图标 + 明确文字”入口和一致选中态；项目设置等重要入口不得只藏在无文字图标中。
- 可编辑的工作区、画布和对象名称必须常显铅笔图标并支持单击编辑；双击和右键只能作为快捷方式，不能作为唯一发现路径。画布顶栏名称采用纯文字原位编辑，只显示文本光标，不使用大面积输入框、下划线或自动焦点框破坏标题层级。
- 私有样式优先用 Tailwind 或少量内联样式；`globals.css` 只放变量、重置、通用样式和必要的第三方覆盖。
- 业务列表、生成记录、媒体和大 JSON 使用 `localforage`；`localStorage` 只保存极小配置。个人渠道 API Key 不得混入 Zustand 普通配置快照：新密钥默认按账号放入 `sessionStorage`，只有用户明确选择“在此设备记住”才进入独立的账号级持久密钥区；退出或切换账号必须清除会话密钥，配置仓库只能在服务端确认身份后恢复。
- 在线画布 Agent 的每条用户消息必须先选择并确认文本推理调用预算，保留调用次数与单轮输出 token 硬上限和持久进度记录；达到预算、拒绝工具、524/504 或连接状态不确定时不得自动发送下一轮。HTTP/SSE 已完整结束但没有可用文本或完整工具调用、`tool_choice=required` 未返回工具，以及 `finish_reason=length`、Responses incomplete、Gemini `MAX_TOKENS` 等明确截断状态，也必须计入本轮调用并停止，不得执行画布操作或用半截/旧工具结果伪装成功。Kimi 等协议要求回传的 `reasoning_content` 只能作为内存中的工具调用协议状态原样带入下一轮，禁止展示、写入会话消息或诊断日志。等待工具确认期间必须锁定其他 Agent 消息，避免多轮交错和费用归属不清。
- 在线画布 Agent 的停止只代表中断浏览器连接，必须明确提示供应商可能仍执行和计费；请求或工具上下文未结束时不得静默卸载面板或页面，卸载后也不得再发起下一轮模型请求。后台持久影视会话只停止前端查询，不得伪装成取消供应商任务。
- 本地 Canvas Agent 的连接 Token 只允许通过启动 URL fragment 交接，读取后立即清除并仅保存到 `sessionStorage`；禁止进入查询参数、`localStorage`、画布同步、导出和诊断日志。网页只允许连接 `127.0.0.1`、`localhost` 或 `::1`，HTTP 与 SSE 请求统一使用 `X-Canvas-Agent-Token`，本地服务不得接受 `?token=`；自动连接不得关闭工具写操作确认。
- 文本模型测活只能使用固定低风险业务校验提示词，并设置极小输出 token 硬上限，禁止演变为任意免费提示词或无界长回答。每次浏览器提交必须先持久化随机提交键，后端以用户与提交键永久返回原探针；接管其他标签页或管理员的活动探针时，也必须先把当前提交键持久绑定到原任务，即使接管响应丢失时任务已经终结也不得创建第二次调用。同一自定义配置在同一用户下只能有一个排队/运行探针，同一系统渠道与模型还必须跨管理员全局互斥。顺序重发、双击、多标签页和多实例并发都返回原任务且不得再次访问供应商，收到明确创建响应后的下一次点击才可生成新提交键。流式结论必须与实际渠道请求配置绑定并设置有效期；合法 SSE 格式和完成标记不足以证明长连接安全，只有实际观察到响应正文分批、跨时间到达才能记为长任务“适合慢请求”的诊断，单包或疑似缓冲 SSE 必须单独标记并在费用确认中提示风险。系统渠道只公开状态、传输方式、耗时和检查时间，禁止公开配置指纹、密钥或响应正文。测活只服务管理员查看渠道可用性，不是普通用户的调用授权：系统渠道和自定义渠道即使未测活、失败、过期或工具诊断失败，用户仍可在明确预算/费用确认后发起调用和显式重试；页面与后端不得因探针状态直接拦截。真实供应商错误、超时、截断、连接中断、费用待核对和工具未返回等结果仍按原有安全规则停止后续轮次，禁止把半截结果当成功。
- 系统与自定义同步模型中转必须共用后台可配置的总时限；系统渠道等待并发槽位也计入总时限，槽位 TTL 必须覆盖请求时限。等待槽位超时发生在供应商调用和计费前，应明确说明无费用；上游请求发出后的超时或中断则必须进入费用待核对。
- 图片、视频和音频的创建、轮询必须只走后端持久化任务，不得新增浏览器直连供应商，也不得把媒体生成路径放入系统或 `/api/ai/custom` 同步代理白名单；浏览器保留的在线 Agent 文本和模型目录请求统一使用 `channelRequest`。这样才能同时保留任务幂等、计费状态、跨刷新恢复、密钥隔离和同源 CORS 边界。
- 生产 Web 必须统一发送 CSP、`nosniff`、点击劫持、Referrer 与最小浏览器权限策略；HSTS 只能在可信反向代理明确传入 HTTPS 协议时发送。CSP 调整必须同时验证 Blob Worker、MediaPipe/FFmpeg WASM、外部 OSS 媒体、本地回环 Canvas Agent 与 SSE，禁止为修兼容直接放开 `unsafe-eval` 或任意内联脚本。

## 画布 UI

- 画布与核心产品界面的视觉交互以 Aceternity UI 的空间层次和组件语言为主基线；优先适配其组件源码并结合当前业务重构，不以旧平面组件外加少量动画作为完成标准。
- Aceternity 组件必须改造成当前项目的命令、状态和主题契约，使用 `lucide-react` 与现有业务 hook；不得为同一入口长期并行维护新旧两套视觉实现。
- 遵循现有画布主题，优先使用 `canvasThemes`、`useThemeStore` 和 Ant Design token，不硬编码导致明暗主题失配的颜色。
- 工具 Dock、节点表面、弹窗和浮层应具有清晰空间层级；高频创作操作保持紧凑，动效强度按使用频率和画布性能调整，但不退回静态平面方案。
- 图片节点默认保持原始比例；批量生成、多图和助手面板不得长期遮挡主要画布空间。
- 动效服务于状态变化和空间关系，并尊重 `prefers-reduced-motion`；不要添加持续干扰创作的装饰动画。

## 文档

- README 只保留项目定位、核心功能、快速开始、数据说明和文档入口。
- `docs/index.md` 是面向 AI 的短索引；详细内容只写入 `docs/content/docs/` 对应页面。
- 功能说明、代码地图、待办和待测试分别维护在 `features.mdx`、`code-map.mdx`、`todo.mdx`、`pending-test.mdx`。
- 已实现但未由用户确认的变化写入 `pending-test.mdx`；确认后再更新正式功能说明。
- 完成 todo 后先移入 `pending-test.mdx`，不要直接写入正式功能说明。
- `CHANGELOG.md` 的 `Unreleased` 只做版本级归纳，不逐条复制待测试内容。
- 每次任务结束前检查 todo 和 pending-test；没有功能变化时无需机械修改。
- 文档默认中文，不写过期日期，不暴露密钥、Token 或机器敏感信息。

## 本地启动

- Windows 零基础启动统一使用 `scripts/start-local.ps1`。脚本必须在构建前检查 Docker Linux Engine、Compose 能力和实际 Web 监听端口；诊断时不得自动启动 Docker Desktop、删除卷或先创建半套容器。
- `CANVAS_HTTP_PORT` 必须同时驱动 Compose 端口映射、冲突检查、健康检查地址和最终访问地址。原有本项目 Web 容器可继续占用同一端口；其他进程占用时必须在 `compose up` 前停止并给出换端口方法。

## 部署维护

- 生产备份前必须由管理员显式确认没有活动或状态不确定的供应商请求，并在关闭公开入口后再次从数据库核对 `queued` / `running` 任务和 `reserved` / `running` 系统计费订单为零；个人自定义渠道同步调用仍须人工核对 Backend 与供应商后台，不得为完成备份直接中断可能计费的请求。
- 备份必须包含 PostgreSQL/完整 SQLite 数据、Backend 数据与 `.settings-key`、部署环境、实际版本和校验清单，并明确 OSS 另行备份、Redis 协调状态不作事实恢复。自动脚本必须保留失败现场、阻止并发备份和递归备份，并在退出时恢复备份前的服务运行状态；不能把半成品标记为有效恢复点。

## Git Commit 文案

提交说明必须使用统一格式（不要模仿仓库里旧的纯英文 subject）：

```text
<type>(<scope>): <业务模块> - <变更摘要>
```

- `type`：`feat` | `fix` | `refactor` | `perf` | `docs` | `test` | `build` | `ci` | `chore` | `revert`
- `scope`：技术域英文；跨模块用 `*`。常用：`canvas`、`platform`、`auth`、`api`、`database`、`deploy`、`release`、`dependencies`、`security`、`media`、`admin`
- `业务模块`：中文可检索名（如「模型选择」「登录态」），不用纯文件名
- `变更摘要`：中文写业务结果；不堆文件列表；不加句末句号；专有名词可英文
- 默认不写 body；breaking / 复杂原因 / issue 再写
- 正例：`fix(canvas): 模型选择 - 清空可选项后仍残留已删除模型显示为 undefined`
- 正例：`chore(release): 版本发布 - publish v0.16.4`
- 反例：`fix(canvas): use supported modal style slot`（纯英文）、`fix: 修了 bug`（缺 scope 与模块）

## 发布

- 发布时将 `Unreleased` 整理为新版本并保留空标题，更新 `VERSION`，提交全部当前改动，再创建对应 `vX.Y.Z` tag。
- 发布 commit 使用：`chore(release): 版本发布 - publish vX.Y.Z`
- 发布流程不执行编译、测试或构建，除非用户明确要求。

## 当前边界

- 画布和素材支持登录后端同步；`localForage` 仍是缓存及后端/OSS 不可用时的降级存储。
- 用户 AI API Key 默认只保存在当前浏览器标签页会话，用户可在可信个人设备上明确选择持久保存；任务创建时可能提交到自部署后端。安全说明必须强调 HTTPS、可信部署、浏览器扩展/XSS 风险以及独立密码管理器备份。
- 媒体资源支持私有 OSS 或后端文件存储；删除节点或素材不会自动清理 OSS 远端对象。
- Docker 静态资源和生产部署仍需按待测试清单验证，不得写成已全面生产验证。
