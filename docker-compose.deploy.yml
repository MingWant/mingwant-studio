services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mingwant_studio}
      POSTGRES_USER: ${POSTGRES_USER:-mingwant_studio}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?请先运行一键部署脚本生成数据库密码}
    volumes:
      - ${CANVAS_POSTGRES_DATA_PATH:-postgres-data}:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  redis:
    image: redis:7.4-alpine
    command: ["redis-server", "--appendonly", "yes", "--appendfsync", "everysec"]
    volumes:
      - ${CANVAS_REDIS_DATA_PATH:-redis-data}:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  backend:
    image: ${MINGWANT_BACKEND_IMAGE:-mingwant-studio-backend:server}
    environment:
      CANVAS_BACKEND_ADDR: ":8080"
      CANVAS_BACKEND_DATA_DIR: /data
      CANVAS_DATABASE_DRIVER: postgres
      DATABASE_URL: ${DATABASE_URL:?请先运行一键部署脚本生成数据库连接}
      REDIS_URL: redis://redis:6379/0
      CANVAS_WORKER_CONCURRENCY: ${CANVAS_WORKER_CONCURRENCY:-3}
      CANVAS_CHANNEL_CONCURRENCY: ${CANVAS_CHANNEL_CONCURRENCY:-3}
      CANVAS_SHUTDOWN_DRAIN_TIMEOUT: ${CANVAS_SHUTDOWN_DRAIN_TIMEOUT:-40m}
      CANVAS_CONTAINER_STOP_GRACE_PERIOD: ${CANVAS_CONTAINER_STOP_GRACE_PERIOD:-45m}
      CANVAS_REGISTRATION_ENABLED: ${CANVAS_REGISTRATION_ENABLED:-false}
      CANVAS_ALLOW_PRIVATE_UPSTREAMS: ${CANVAS_ALLOW_PRIVATE_UPSTREAMS:-false}
      CANVAS_ALLOWED_PRIVATE_UPSTREAM_HOSTS: ${CANVAS_ALLOWED_PRIVATE_UPSTREAM_HOSTS:-}
      CANVAS_CORS_ORIGINS: ${CANVAS_CORS_ORIGINS:-}
      GIN_MODE: release
    volumes:
      - ${CANVAS_DATA_PATH:-backend-data}:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    stop_grace_period: ${CANVAS_CONTAINER_STOP_GRACE_PERIOD:-45m}
    restart: unless-stopped

  web:
    image: ${MINGWANT_WEB_IMAGE:-mingwant-studio-web:server}
    ports:
      # 生产 Web 只作为本机 HTTPS 反向代理的上游，避免把未加密入口直接暴露到公网。
      - "127.0.0.1:${CANVAS_HTTP_PORT:-3000}:3000"
    depends_on:
      backend:
        condition: service_healthy
    stop_signal: SIGQUIT
    stop_grace_period: ${CANVAS_CONTAINER_STOP_GRACE_PERIOD:-45m}
    restart: unless-stopped

volumes:
  backend-data:
  postgres-data:
  redis-data:
